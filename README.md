Hello guys, to start

1. Clone the repo: `git clone https://github.com/USERNAME/TRIGON-plus.git`
2. Install dependencies: `uv sync`
3. Activate the environment: `source .venv/bin/activate`

Use your branch and push in the main only when you are sure and we checked the code. 
